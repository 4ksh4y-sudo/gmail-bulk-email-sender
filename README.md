# gmail-bulk-email-sender
A simple, single-file Python script for sending personalized bulk emails through **Gmail SMTP** using an **App Password**. Includes dry-run mode, per-recipient message count, rate limiting, and dual logging.
